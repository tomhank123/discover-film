// import { selectMediaDetailsDomain } from '../selectors';

describe('selectMediaDetailsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
