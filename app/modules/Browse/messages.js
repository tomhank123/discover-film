/*
 * Browse Messages
 *
 * This contains all the text for the Browse container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Browse';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Browse container!',
  },
});
