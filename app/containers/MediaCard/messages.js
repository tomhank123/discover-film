/*
 * MediaCard Messages
 *
 * This contains all the text for the MediaCard container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MediaCard';

export default defineMessages({
  watchNow: {
    id: `${scope}.watch_now`,
    defaultMessage: 'Watch Now',
  },
  watchList: {
    id: `${scope}.watch_list`,
    defaultMessage: 'Watch Now',
  },
});
