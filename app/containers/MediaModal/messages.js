/*
 * MediaModal Messages
 *
 * This contains all the text for the MediaModal container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MediaModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MediaModal container!',
  },
});
