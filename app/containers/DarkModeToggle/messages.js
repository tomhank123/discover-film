/*
 * DarkModeToggle Messages
 *
 * This contains all the text for the DarkModeToggle component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DarkModeToggle';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DarkModeToggle component!',
  },
});
