/*
 * CombinedModal Messages
 *
 * This contains all the text for the CombinedModal container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CombinedModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CombinedModal container!',
  },
});
