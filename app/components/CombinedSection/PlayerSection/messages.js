/*
 * PlayerSection Messages
 *
 * This contains all the text for the PlayerSection component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PlayerSection';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PlayerSection component!',
  },
});
