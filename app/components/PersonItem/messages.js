/*
 * Header Messages
 *
 * This contains all the text for the Header component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PersonItem';

export default defineMessages({
  moreInfo: {
    id: `${scope}.more_info`,
    defaultMessage: 'More Info',
  },
});
