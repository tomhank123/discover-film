/*
 * ReviewCard Messages
 *
 * This contains all the text for the ReviewCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ReviewCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReviewCard component!',
  },
});
