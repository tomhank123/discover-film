/*
 * SimilarFrame Messages
 *
 * This contains all the text for the SimilarFrame component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SimilarFrame';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SimilarFrame component!',
  },
});
