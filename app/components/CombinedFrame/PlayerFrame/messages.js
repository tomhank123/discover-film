/*
 * PlayerFrame Messages
 *
 * This contains all the text for the PlayerFrame component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PlayerFrame';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PlayerFrame component!',
  },
});
