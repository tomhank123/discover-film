/*
 * SimilarSection Messages
 *
 * This contains all the text for the SimilarSection component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SimilarSection';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SimilarSection component!',
  },
});
