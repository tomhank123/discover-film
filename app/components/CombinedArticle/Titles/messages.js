/*
 * Header Messages
 *
 * This contains all the text for the Header component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Titles';

export default defineMessages({
  watchNow: {
    id: `${scope}.watch_now`,
    defaultMessage: 'Watch Now',
  },
  watchList: {
    id: `${scope}.watch_list`,
    defaultMessage: 'Watch Now',
  },
});
