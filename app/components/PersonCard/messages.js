/*
 * PersonCard Messages
 *
 * This contains all the text for the PersonCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PersonCard';

export default defineMessages({
  moreInfo: {
    id: `${scope}.more_info`,
    defaultMessage: 'More Info',
  },
});
